# Third-party libraries

This extension does **not** use any third-party JavaScript libraries in the Bridge V1 phase.

Bridge V1 (`listAccounts`, `sendEmail`, `replyEmail`, `findMessages`, `restoreToInbox`) uses only Thunderbird WebExtension APIs documented at https://webextension-api.thunderbird.net/en/mv3/
